Now in our tui, as you can see, we are showing a message list with the local ggf model run so it will still be there. We are going to merge this tui with codex rust cli, which has other rendering inline tui rendering. That's why we are going to make a message list screen to have another varient that already supports the codex rust cli.
Please do a website about codextras cli in its inline chat message rendering as of 25 March 2026. While we keep our local message list rendering correctly the same, in our message list rendering screen we also add codextras tui rendering. We are going to just remove the branding and inline menus and just show the codextras message list inline, like it was before. We just want to add the codextras tui with our tui as fast as possible.
Now kindly do a search about Codex-rust CLI inline tui and in our current tui message list how we can integrate it. We just have to remove the Codex-rust tui related UI menu and branding and just render the message list as fast as possible because I want to finish this project as fast as possible. 

Now then set it up as and we are using "just dx" command as we an't afford to keep rebuilding as we only use run as its taking too much time and also keep it in mind to use low jobs so that it can runned on our this low-end device!!!

Now I have foked codex-rust cli so please tell out of 10 difficulty how hard it can be to bring the chat api so that we can configure other providers inside our codex rust cli - Just think codex rust cli is already a beast and I will add multiprovider support with all its best configs features by default and then make a better tui it will not be a better version of open-codex but it will be the best ai cli ever!!!

Now its still not working so we are gonna do it later - Now we will work to make our codex rust cli with multi-provider insatead of just openai based now please don't give me any code and just tell by doing web search as today is 23 March 2026 and do check this codebase: And out of 10 give me details about how difficult it is to add the multi-provider support on our Codex-rust cli back??

I want to fork codex rust cli to my dx. Now I have a very low RAM and a low-end device so even if I do a small change it is taking too much time to reflect the change. Is there any way in rush so that only the changed crate or part gets re-run instead of running everything and building everything from scratch again and again? I know cargo run works but is there any better way than cargo run that only runs what was changed? As my current low-end device is taking too much time to reflect the change, please give me the best rush crate so that it can only run the changed part as fast as possible even in a low-end device. As today is 23rd March 2026 

Please look at the root PROVIDERS.md file and implement it and please run cargo run to check don't do cargo build/test/check as this project in our low-end device can only hangle run command!!!

And isn't theres a way to tell codex to try to connect to all providers with all key listed in the .env so that it can list one default models from every .env keys from the .env file and then have configure tui so that we can choose other model from the providers
Currently the Codex that tui default is local LLM but local LLM has some errors while connecting to the model so that's why. Please by default connect to our multi-provider Mistral API from the API key in the.env root file. Connect with the Mistral backend and face all the models from the Mistral API and choose the first model as the default model for Codex-rust CLI. 

Now success - the multiproviders are working - now as we added mistral now can we add all providers as most of the providers are openai api and these mistral like chat response so can't we connect to any providers from now on?? if so please connect to all of the provdiers that have api keys in the root .env file

Now make sure we can add any OpenAI-compatible providers. When we add them, we automatically fetch the latest model from each provider and set it as the default for that provider so that the user doesn't have to manually add the provider details. They just add the API key and we fetch all the things that are needed for it to work.

Now I want you to look at the "F:\codex\codex-rs\dx" path and its an standalone rust project
